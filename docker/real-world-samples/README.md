# Real-World Samples

Checked-in real-world runtime reports used for regression tests and classifier validation live here.

These files should come from reviewed, redacted, real environments. They are not synthetic fixtures unless a file is explicitly documented as such.
